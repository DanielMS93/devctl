---
phase: 05-tasks-and-dependencies
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - cmd/devctl/task.go
  - cmd/devctl/deps.go
  - cmd/devctl/main.go
autonomous: true

must_haves:
  truths:
    - "User can create a task with devctl tasks create <description>"
    - "User can list tasks with devctl tasks list showing state and description"
    - "User can update task state with devctl tasks update <id> --state running"
    - "User can update task branch with devctl tasks update <id> --branch feature/x"
    - "User can delete a task with devctl tasks delete <id>"
    - "User can add a dependency with devctl deps add <task-id> <depends-on-id>"
    - "User can remove a dependency with devctl deps remove <task-id> <depends-on-id>"
    - "User can list dependencies with devctl deps list"
    - "Circular dependency is rejected with a clear error message"
  artifacts:
    - path: "cmd/devctl/task.go"
      provides: "devctl tasks subcommands"
      contains: "var taskCmd"
    - path: "cmd/devctl/deps.go"
      provides: "devctl deps subcommands with cycle detection"
      contains: "var depsCmd"
    - path: "cmd/devctl/main.go"
      provides: "Registration of task and deps commands"
      contains: "taskCmd"
  key_links:
    - from: "cmd/devctl/task.go"
      to: "internal/task/store.go"
      via: "TaskStore methods"
      pattern: "task\\.NewStore"
    - from: "cmd/devctl/deps.go"
      to: "internal/dependency/store.go"
      via: "DepStore methods"
      pattern: "dependency\\.NewStore"
    - from: "cmd/devctl/deps.go"
      to: "cycle detection logic"
      via: "WouldCreateCycle check before Add"
      pattern: "WouldCreateCycle|wouldCreateCycle"
---

<objective>
Wire task and dependency CRUD to the CLI via Cobra subcommands, including cycle detection on dependency insertion.

Purpose: Users need a CLI interface to manage tasks and declare dependencies before the dashboard can visualize them.
Output: cmd/devctl/task.go, cmd/devctl/deps.go, updated main.go
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-tasks-and-dependencies/05-RESEARCH.md
@.planning/phases/05-tasks-and-dependencies/05-01-SUMMARY.md
@cmd/devctl/repo.go
@cmd/devctl/worktree.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: devctl tasks subcommands</name>
  <files>cmd/devctl/task.go, cmd/devctl/main.go</files>
  <action>
Create cmd/devctl/task.go following the exact Cobra pattern from repo.go:

Parent command:
- `var taskCmd = &cobra.Command{Use: "tasks", Short: "Manage tasks"}`

Subcommands (all extract db via `cmd.Context().Value(dbKey{}).(*sqlx.DB)`):

1. `tasks create <description>` -- Creates task via TaskStore.Create(). Print "Created task {id[:8]}: {description}". Accept optional --repo flag (repo name or path, resolved via resolveRepo helper from repo.go), --branch flag (string).

2. `tasks list` -- Lists all tasks via TaskStore.List(). Format each as: `  [{id[:8]}] {state:8s} {description}  {branch if set}`. Accept optional --repo flag to filter by repo.

3. `tasks update <id>` -- Updates task via TaskStore.Update(). Accept --state flag (validate: queued/running/completed only; reject "blocked" with error "blocked is computed automatically, not set manually"), --branch flag. At least one flag required. Use TaskStore.Get() with prefix matching so user can pass short ID (e.g., "a1b2c3d4").

4. `tasks delete <id>` -- Deletes task via TaskStore.Delete(). Print "Deleted task {id[:8]}". Use prefix matching for short IDs.

Register taskCmd on rootCmd in main.go (add `rootCmd.AddCommand(taskCmd)` alongside existing repo/worktree commands).

In init():
- taskCreateCmd flags: --repo (string), --branch (string)
- taskListCmd flags: --repo (string)
- taskUpdateCmd flags: --state (string), --branch (string)
- taskCmd.AddCommand(taskCreateCmd, taskListCmd, taskUpdateCmd, taskDeleteCmd)
  </action>
  <verify>`go build ./cmd/devctl/...` compiles. `go vet ./cmd/devctl/...` passes.</verify>
  <done>All four task subcommands compile and are registered on rootCmd.</done>
</task>

<task type="auto">
  <name>Task 2: devctl deps subcommands with cycle detection</name>
  <files>cmd/devctl/deps.go</files>
  <action>
Create cmd/devctl/deps.go following the same Cobra pattern:

Parent command:
- `var depsCmd = &cobra.Command{Use: "deps", Short: "Manage task dependencies"}`

Subcommands:

1. `deps add <task-id> <depends-on-id>` -- Args: cobra.ExactArgs(2). Resolve both IDs via TaskStore.Get() (prefix match). Before calling DepStore.Add(), run cycle detection: fetch all existing deps with DepStore.ListAll(), then call wouldCreateCycle(deps, taskID, dependsOnID). If cycle detected, return error: "cannot add dependency: would create cycle {taskID[:8]} -> ... -> {dependsOnID[:8]} -> {taskID[:8]}". On success print "Added: {taskID[:8]} depends on {dependsOnID[:8]}".

2. `deps remove <task-id> <depends-on-id>` -- Args: cobra.ExactArgs(2). Resolve IDs, call DepStore.Remove(). Print "Removed: {taskID[:8]} no longer depends on {dependsOnID[:8]}".

3. `deps list` -- Lists all dependency links via DepStore.ListAll(). Also fetch all tasks to show descriptions. Format: `  {taskID[:8]} ({desc}) depends on {depID[:8]} ({desc})`. Accept optional --task flag to filter by one task.

Cycle detection function (unexported, in deps.go):
```go
func wouldCreateCycle(deps []dependency.Dep, taskID, dependsOnID string) bool {
    // Build adjacency: task -> what it depends on
    upstream := make(map[string][]string)
    for _, d := range deps {
        upstream[d.TaskID] = append(upstream[d.TaskID], d.DependsOnID)
    }
    // DFS from dependsOnID following upstream edges to see if taskID is reachable
    visited := make(map[string]bool)
    var dfs func(string) bool
    dfs = func(node string) bool {
        if node == taskID { return true }
        if visited[node] { return false }
        visited[node] = true
        for _, up := range upstream[node] {
            if dfs(up) { return true }
        }
        return false
    }
    return dfs(dependsOnID)
}
```

Register depsCmd on rootCmd in main.go (add alongside taskCmd).

In init():
- depsListCmd flags: --task (string)
- depsCmd.AddCommand(depsAddCmd, depsRemoveCmd, depsListCmd)
  </action>
  <verify>`go build ./cmd/devctl/...` compiles. `go vet ./cmd/devctl/...` passes.</verify>
  <done>All three deps subcommands compile, cycle detection implemented, registered on rootCmd.</done>
</task>

</tasks>

<verification>
- `go build ./...` succeeds
- `devctl tasks --help` shows create/list/update/delete subcommands
- `devctl deps --help` shows add/remove/list subcommands
- Cycle detection logic prevents A->B->A insertion
</verification>

<success_criteria>
Full task and dependency CLI is operational. Users can create tasks, set states, declare dependencies, and the system rejects circular dependencies with clear errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05-tasks-and-dependencies/05-02-SUMMARY.md`
</output>
