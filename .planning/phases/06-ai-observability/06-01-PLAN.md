---
phase: 06-ai-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/storage/migrations/005_agent_patches.up.sql
  - pkg/storage/migrations/005_agent_patches.down.sql
  - internal/agent/store.go
  - internal/agent/store_test.go
  - internal/agent/config.go
autonomous: true

must_haves:
  truths:
    - "Migration 005 creates agent_runs and agent_patches tables with correct schema"
    - "AgentRunStore and PatchStore provide CRUD for agent runs and patches"
    - "Agent config loads from viper with sensible defaults"
  artifacts:
    - path: "pkg/storage/migrations/005_agent_patches.up.sql"
      provides: "agent_runs and agent_patches tables"
      contains: "CREATE TABLE"
    - path: "internal/agent/store.go"
      provides: "AgentRunStore and PatchStore with CRUD methods"
      exports: ["AgentRunStore", "PatchStore", "AgentRun", "AgentPatch"]
    - path: "internal/agent/config.go"
      provides: "AgentConfig loaded from viper"
      exports: ["AgentConfig", "LoadConfig"]
  key_links:
    - from: "internal/agent/store.go"
      to: "pkg/storage/migrations/005_agent_patches.up.sql"
      via: "SQL queries match table schema"
      pattern: "agent_runs|agent_patches"
---

<objective>
Create the data layer foundation for agent workflows: migration 005 (agent_runs + agent_patches tables), Go store types with CRUD operations, and viper-based agent configuration.

Purpose: Every other agent-related plan depends on the data store and config. This must exist first.
Output: Migration files, AgentRunStore, PatchStore, AgentConfig
</objective>

<execution_context>
@/Users/daniel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/daniel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ai-observability/06-RESEARCH.md
@internal/task/store.go
@internal/dependency/store.go
@pkg/storage/migrations/004_tasks.up.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 005 + AgentRunStore and PatchStore</name>
  <files>
    pkg/storage/migrations/005_agent_patches.up.sql
    pkg/storage/migrations/005_agent_patches.down.sql
    internal/agent/store.go
    internal/agent/store_test.go
  </files>
  <action>
Create migration 005_agent_patches.up.sql with the schema from RESEARCH.md:
- `agent_runs` table: id TEXT PK, repo_path TEXT NOT NULL, branch TEXT NOT NULL, workflow TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'pending' (pending/running/completed/failed), triggered_at INTEGER NOT NULL, completed_at INTEGER, error_msg TEXT. Indexes on (repo_path, branch) and (status).
- `agent_patches` table: id TEXT PK, run_id TEXT NOT NULL REFERENCES agent_runs(id) ON DELETE CASCADE, repo_path TEXT NOT NULL, branch TEXT NOT NULL, title TEXT NOT NULL, description TEXT, patch_data TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'draft' (draft/approved/applied/rejected/reverted), created_at INTEGER NOT NULL, reviewed_at INTEGER, applied_at INTEGER. Indexes on (status) and (run_id).
- Down migration drops both tables.

Create `internal/agent/store.go`:
- Define Go structs: `AgentRun` and `AgentPatch` with `db:` tags matching columns.
- `AgentRunStore` struct wrapping `*sqlx.DB` with methods: `Create(ctx, run)`, `Get(ctx, id)`, `UpdateStatus(ctx, id, status, errorMsg)`, `ListByBranch(ctx, repoPath, branch)`, `LastTriggered(ctx, repoPath, branch, workflow) (time.Time, error)`.
- `PatchStore` struct wrapping `*sqlx.DB` with methods: `Create(ctx, patch)`, `Get(ctx, id)` (supports UUID prefix match like TaskStore), `ListByStatus(ctx, status)`, `ListByRun(ctx, runID)`, `UpdateStatus(ctx, id, status)`, `SetApplied(ctx, id)`, `SetReverted(ctx, id)`.
- Use `github.com/google/uuid` for ID generation (already in go.mod from task store).
- Follow the exact same patterns as `internal/task/store.go` (sqlx, context, error handling).

Create `internal/agent/store_test.go`:
- Test Create + Get for both stores.
- Test PatchStore.ListByStatus returns only matching patches.
- Test Get with UUID prefix match.
- Use the same test DB setup pattern as task/store_test.go.
  </action>
  <verify>
`cd /Users/daniel/Projects/devctl && go test ./internal/agent/ -v -count=1` passes all tests.
`go vet ./internal/agent/` reports no issues.
  </verify>
  <done>Both stores compile, tests pass, migration files exist with correct schema.</done>
</task>

<task type="auto">
  <name>Task 2: Agent configuration via viper</name>
  <files>internal/agent/config.go</files>
  <action>
Create `internal/agent/config.go`:
- Define `AgentConfig` struct with fields:
  - `Enabled bool` — master switch (default true)
  - `IdleThresholdMinutes int` — minutes before idle trigger (default 20)
  - `CooldownMinutes int` — minutes between re-triggers for same branch (default 60)
  - `MaxPatchSizeKB int` — reject patches larger than this (default 1024)
  - `Workflows map[string]WorkflowConfig` — per-workflow enable/disable + custom command
  - `DisabledRepos []string` — repos to exclude from idle detection
- Define `WorkflowConfig` struct: `Enabled bool`, `Command string` (shell command to run).
- `LoadConfig() AgentConfig` function that reads from viper with key prefix `agent.`:
  - `agent.enabled` (bool, default true)
  - `agent.idle_threshold_minutes` (int, default 20)
  - `agent.cooldown_minutes` (int, default 60)
  - `agent.max_patch_size_kb` (int, default 1024)
  - `agent.workflows.code_review.enabled` (bool, default true)
  - `agent.workflows.code_review.command` (string, default "claude --print 'Review this branch for issues'")
  - `agent.workflows.test_generation.enabled` (bool, default false)
  - `agent.disabled_repos` (string slice, default empty)
- Add helper `IsRepoDisabled(repoPath string) bool` that checks DisabledRepos list.
- Add helper `IdleThreshold() time.Duration` and `Cooldown() time.Duration` that convert minutes to duration.
  </action>
  <verify>`go build ./internal/agent/` compiles without errors.</verify>
  <done>AgentConfig loads from viper with all defaults, helpers return correct durations.</done>
</task>

</tasks>

<verification>
- `go test ./internal/agent/ -v -count=1` — all store tests pass
- `go vet ./internal/agent/` — no issues
- `go build ./...` — full project compiles
- Migration files exist at pkg/storage/migrations/005_agent_patches.{up,down}.sql
</verification>

<success_criteria>
- agent_runs and agent_patches tables defined in migration 005
- AgentRunStore and PatchStore provide full CRUD
- AgentConfig loads from viper with sensible defaults
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-ai-observability/06-01-SUMMARY.md`
</output>
